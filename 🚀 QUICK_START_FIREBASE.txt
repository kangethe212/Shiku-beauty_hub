════════════════════════════════════════════════════════════════════
🚀 QUICK START - FIREBASE + GOOGLE CLOUD RUN DEPLOYMENT
════════════════════════════════════════════════════════════════════

✅ FILES CREATED:
────────────────────────────────────────────────────────────────────
   ✅ Dockerfile          - Container configuration
   ✅ .dockerignore       - Files to exclude from Docker
   ✅ firebase.json       - Firebase Hosting configuration
   ✅ cloudbuild.yaml     - Automated build configuration
   ✅ settings.py         - Updated for Firebase domains
   ✅ deploy_firebase.bat - Deployment script
   ✅ Complete guide      - 🚀 FIREBASE_DEPLOYMENT_GUIDE.md

────────────────────────────────────────────────────────────────────
📋 STEP 1: INSTALL REQUIRED TOOLS
────────────────────────────────────────────────────────────────────

1. Install Google Cloud SDK:
   https://cloud.google.com/sdk/docs/install

2. Install Firebase CLI:
   npm install -g firebase-tools

3. Install Docker:
   https://www.docker.com/products/docker-desktop

4. Verify installations:
   gcloud --version
   firebase --version
   docker --version

────────────────────────────────────────────────────────────────────
🔧 STEP 2: SET UP GOOGLE CLOUD & FIREBASE
────────────────────────────────────────────────────────────────────

1. Create Google Cloud Account:
   https://cloud.google.com/
   (Enable billing - FREE tier available!)

2. Create Firebase Project:
   https://console.firebase.google.com/
   (Link with Google Cloud project)

3. Login to Google Cloud:
   gcloud auth login

4. Login to Firebase:
   firebase login

5. Set your project:
   gcloud config set project YOUR_PROJECT_ID
   firebase use YOUR_PROJECT_ID

────────────────────────────────────────────────────────────────────
🐳 STEP 3: BUILD & DEPLOY (EASY WAY)
────────────────────────────────────────────────────────────────────

OPTION 1: Use the deployment script (Easiest!)

   Double-click: deploy_firebase.bat

OPTION 2: Manual deployment

   1. Collect static files:
      python manage.py collectstatic --noinput

   2. Build Docker image:
      docker build -t gcr.io/YOUR_PROJECT_ID/shiku-beauty-hub:latest .

   3. Push to Container Registry:
      docker push gcr.io/YOUR_PROJECT_ID/shiku-beauty-hub:latest

   4. Deploy to Cloud Run:
      gcloud run deploy shiku-beauty-hub \
        --image gcr.io/YOUR_PROJECT_ID/shiku-beauty-hub:latest \
        --platform managed \
        --region us-central1 \
        --allow-unauthenticated \
        --port 8080 \
        --memory 512Mi \
        --set-env-vars SECRET_KEY=YOUR_SECRET_KEY,DEBUG=False

   5. Deploy Firebase Hosting:
      firebase deploy --only hosting

────────────────────────────────────────────────────────────────────
🗄️ STEP 4: SET UP DATABASE (Cloud SQL)
────────────────────────────────────────────────────────────────────

1. Create Cloud SQL PostgreSQL:
   gcloud sql instances create shiku-db \
     --database-version=POSTGRES_14 \
     --tier=db-f1-micro \
     --region=us-central1

2. Create database:
   gcloud sql databases create shiku_db --instance=shiku-db

3. Create user:
   gcloud sql users create shiku_user \
     --instance=shiku-db \
     --password=YOUR_PASSWORD

4. Get connection name:
   gcloud sql instances describe shiku-db \
     --format="value(connectionName)"

5. Update Cloud Run with database URL:
   gcloud run services update shiku-beauty-hub \
     --region us-central1 \
     --add-cloudsql-instances PROJECT_ID:us-central1:shiku-db \
     --set-env-vars DATABASE_URL=postgresql://shiku_user:PASSWORD@/shiku_db?host=/cloudsql/PROJECT_ID:us-central1:shiku-db

────────────────────────────────────────────────────────────────────
🔄 STEP 5: RUN MIGRATIONS
────────────────────────────────────────────────────────────────────

Create migration job:
   gcloud run jobs create migrate-shiku \
     --image gcr.io/YOUR_PROJECT_ID/shiku-beauty-hub:latest \
     --region us-central1 \
     --command python \
     --args manage.py,migrate \
     --set-env-vars SECRET_KEY=YOUR_SECRET_KEY,DATABASE_URL=YOUR_DATABASE_URL

Execute:
   gcloud run jobs execute migrate-shiku --region us-central1

────────────────────────────────────────────────────────────────────
👤 STEP 6: CREATE ADMIN USER
────────────────────────────────────────────────────────────────────

   gcloud run jobs create createsuperuser-shiku \
     --image gcr.io/YOUR_PROJECT_ID/shiku-beauty-hub:latest \
     --region us-central1 \
     --command python \
     --args manage.py,createsuperuser \
     --interactive \
     --set-env-vars SECRET_KEY=YOUR_SECRET_KEY,DATABASE_URL=YOUR_DATABASE_URL

   gcloud run jobs execute createsuperuser-shiku --region us-central1

────────────────────────────────────────────────────────────────────
🌐 STEP 7: ACCESS YOUR WEBSITE
────────────────────────────────────────────────────────────────────

Your Firebase URL:
   https://YOUR_PROJECT_ID.web.app

Your Cloud Run URL (Direct):
   https://shiku-beauty-hub-xxxxx-uc.a.run.app

Admin Panel:
   https://YOUR_PROJECT_ID.web.app/admin/
   Username: admin
   Password: (what you set)

────────────────────────────────────────────────────────────────────
💰 FREE TIER LIMITS
────────────────────────────────────────────────────────────────────

Google Cloud Run:
   ✅ 2 million requests/month FREE
   ✅ 400,000 GB-seconds compute FREE
   ✅ 200,000 CPU-seconds FREE

Cloud SQL:
   ✅ First 2 instances FREE (db-f1-micro)

Firebase Hosting:
   ✅ 10 GB storage FREE
   ✅ 360 MB/day bandwidth FREE

────────────────────────────────────────────────────────────────────
📚 FULL GUIDE
────────────────────────────────────────────────────────────────────

For detailed instructions, see:
   🚀 FIREBASE_DEPLOYMENT_GUIDE.md

────────────────────────────────────────────────────────────────────
🆘 TROUBLESHOOTING
────────────────────────────────────────────────────────────────────

1. "Service not found":
   - Check Cloud Run service name matches firebase.json
   - Verify service is deployed in same region

2. Database connection errors:
   - Check Cloud SQL instance is running
   - Verify connection name format
   - Check firewall rules

3. Static files not loading:
   - Run collectstatic before deploying
   - Check STATIC_ROOT in settings.py

4. Need help?
   - Google Cloud Docs: https://cloud.google.com/docs
   - Firebase Docs: https://firebase.google.com/docs

────────────────────────────────────────────────────────────────────

🎉 READY TO DEPLOY! START WITH STEP 2! 🚀💎✨

────────────────────────────────────────────────────────────────────

